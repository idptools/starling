

type: discrete # options: continuous, discrete

discrete:
  beta_scheduler: cosine
  timesteps: 1000
  set_lr: 0.0001
  config_scheduler: LinearWarmupCosineAnnealingLR
  min_snr_loss: False
  min_snr_gamma: 5.0
  warmup_fraction: 0.0
  ddpm_loss_weight: 5.0

continuous:
  noise_schedule: karras
  min_snr_loss_weight: false
  min_snr_gamma: 5
  set_lr: 0.00001
  config_scheduler: CosineAnnealingLR


vae_checkpoint: /home/borna/.starling_weights/model-kernel-epoch=99-epoch_val_loss=1.72.ckpt