# readthedocs.yml
version: 2

sphinx:
  configuration: docs/conf.py

python:
  version: "3.11"
  install:
    - requirements: docs/requirements.txt  
    - method: pip
      path: .

build:
  commands:
    - python -m versioningit  # Ensures _version.py is generated (if using versioningit)